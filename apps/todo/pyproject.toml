[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "synapse-todo"
version = "0.1.0"
description = "Terminal interviews for pending Synapse specs"
requires-python = ">=3.10"

[project.scripts]
TODO = "apps.todo.cli:main"

[tool.setuptools]
packages = ["apps.todo"]
package-dir = { "apps.todo" = "." }
