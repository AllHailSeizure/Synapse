## Summary

<!-- What this PR does, one or two sentences. -->

## Related issues

<!-- Keep what applies, delete the rest. -->
Closes #
References #

## Stack

<!-- Delete this section entirely if this PR is not part of a stack. -->
Part of a stack. Base: `<branch>` — the PR this one builds on, not `master`.
Merges only after its base lands, and only on Nate's explicit go-ahead — never auto-merged.

## Labels

<!-- Applied via `gh pr create --label`, not this template body — listed here so type/priority are visible in the diff. -->
Type: `feat` | `fix` | `refactor` | `test` | `docs` | `tooling` | `assets`
Priority: `p0` | `p1` | `p2` | `p3`
